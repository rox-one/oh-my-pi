The user already approved these commands:
{{#each approved}}
- {{this}}
{{/each}}

New command needing approval:
{{candidate}}

Similar means the same operation on the same kind of target: the approved command with different arguments of the same sort, or the same action on a sibling path. A different program, a different subcommand, reading instead of writing or deleting, a different scope, or anything destructive means not similar.

Reply exactly one word: YES if similar, NO otherwise.
